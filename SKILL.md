# 自动暂存本次新建源码文件（Git Add）

## 目标

在当前任务中，若代理**新建了可版本化源码文件**，应在写入完成后立即执行 `git add <path>`，无需用户额外提醒。  
仅暂存与本任务相关的新文件，避免把无关改动带入暂存区。

---

## 触发条件（全部满足）

1. 文件是**本次任务新建**（未跟踪 / newly created）。
2. 文件属于**源码或配置源码**（如 Java、Mapper XML、DTO 等）。
3. 文件与当前任务目标直接相关。

---

## 默认纳入范围

满足任一条件即可纳入（前提：确为新建）：

- 路径/包名包含：`entity`、`model`、`domain`、`pojo`、`mapper`、`mapping`、`dto`、`vo`、`bo`、`query`、`request`、`response`、`result`
- 文件名模式：
  - `*Mapper.java`
  - `*Mapper.xml`
  - `*Dto.java` / `*DTO.java`
  - `*VO.java` / `*Vo.java`
- 用户明确要求新建某类/某 mapper/某 dto 等

---

## 明确排除范围

以下内容**绝不自动 add**：

- 构建与产物：``、``、``
- 依赖目录：``、`.m2/` 等
- IDE/本地环境文件：``、``、本地临时文件
- 密钥与敏感配置：``、``、``、凭证文件
- 与当前任务无关的新文件

---

## 执行步骤

1. 识别“本次任务新建文件”。
2. 过滤出“纳入范围”且“非排除范围”的文件。
3. 执行精确暂存：
   - 单个：``
   - 多个：``
4. 在回复中简要说明已暂存哪些新文件（只报路径，不做提交）。

---

## 执行时机

- 每次新文件写入成功后，**尽快执行** ``。
- 若同批次创建多个文件，可在该批次完成后一次性 ``。

---

## 与其他 Git 操作的边界

- 仅执行 ``，**不执行** ``、``。
- 不使用 ``、`` 来扩大暂存范围。
- 不更改 Git 配置，不执行破坏性命令。

---

## 冲突与优先级

当以下规则冲突时，优先级从高到低：

1. 用户明确指令
2. 仓库内项目规则（如 ``、``、团队规范）
3. 本技能默认规则

---

## 失败与兜底

- 若 `` 失败：重试一次并报告失败原因（如路径不存在、权限问题）。
- 若文件是否“任务相关”不明确：先询问用户，再决定是否暂存。
